1. 资源中心-资源配置-操作模板配置：首先决定是否必填，是否可见，如果必填但不可见，只能是fixed类型；fixed类型的字段，必须填写固定值；
2. 组织管理-部门配置-资源操作配置：部门这边指定的某类资源某类操作，只从平台配置的可见的字段中提取；然后如果是
    - fixed类型，只能决定是否展示；
    - string类型，可以配置正则表达式以及默认值；
    - number类型，可以配置上下限，小数位数以及默认值；
    - select类型，可以配置选项kv以及级联约束；（已实现）
    - textarea类型，可以配置默认值；


